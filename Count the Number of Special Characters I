Count the Number of Special Characters I

class Solution:
    def numberOfSpecialChars(self, word: str) -> int:
            count = 0
            for c in set(word) :
                if c. lower() and c. upper() in word:
                    count += 1
            return count